# Auto detect text files and normalize line endings to LF on commit
* text=auto eol=lf

# Ensure shell scripts always have LF endings
*.sh text eol=lf

# Binary files
*.png binary
*.jpg binary
*.ico binary

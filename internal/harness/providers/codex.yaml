name: codex
displayName: Codex CLI
description: OpenAI Codex CLI code generation tool
binary: codex

directories:
  project: .codex
  user: ~/.codex

bundleDir:
  mode: add_dir
  flag: "--add-dir"

assets:
  skillDir: .agents/skills
  agentDir: .codex/agents
  toolConfigFile: .codex/config.toml

mcp:
  format: toml
  configPath: .codex/config.toml
